git config --global user.name 'text'
git cinfig --global user.email 'text'

git init

Add remote repository on HitHub:
git remote add origin text

git status

Add to staging area:
git add .

Remove from staging area:
git reset .

Commit files:
git commit -m 'text'

Send files to GitHub:
git push origin master

